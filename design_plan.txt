1.  **Analyze the current design**: The current design uses a dark theme (`#0a0e17`) with glassmorphism (`rgba(26, 35, 50, 0.7)` with `backdrop-filter: blur()`), warm amber accents (`#f59e0b`), and fonts `Inter` and `Outfit`.
2.  **Choose a new design direction**: Let's go with a modern, fresh, light, nature-inspired agriculture theme. It fits the "cattle breed" context perfectly.
    *   **Colors**: Light background (`#f8fafc` or `#f3f4f6`), primary green (`#16a34a` or `#059669`), secondary earth tones (soft browns/tans like `#d97706` or `#b45309`), clean white cards (`#ffffff`), and dark text for contrast (`#1e293b`).
    *   **Fonts**: Swap `Inter`/`Outfit` for a friendly sans-serif combination, maybe `Poppins` (headings) and `Nunito` or `Roboto` (body) to give it a fresh, modern web app feel.
    *   **Spacing/Cards**: Replace glassmorphism with solid white cards, subtle soft shadows (`box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)`), and slightly larger border radiuses (e.g., `12px` or `16px`).
    *   **Buttons**: Solid colors with slight hover lifting, maybe pill-shaped.
    *   **Navbar**: Clean white or very light green header, sticking to the top with a subtle border/shadow.
    *   **Footer**: Simple, clean.
3.  **Update CSS Variables**: Rewrite the `:root` variables in `App.css` and `index.css` to implement the new theme.
4.  **Update Specific Component Styles**:
    *   Remove `backdrop-filter` from `.glass-card`, `.navbar`, etc.
    *   Adjust text colors for light mode (headings dark, text slightly lighter but dark enough for contrast).
    *   Change the accent gradients to solid greens or green gradients.
    *   Update `.upload-zone` to have a light green/grey dashed border and a soft hover state.
    *   Update badges (high, medium, low) to fit the light theme better.
    *   Ensure mobile responsiveness remains intact (mostly checking media queries, which shouldn't need changing, just colors/paddings).
5.  **Pre-commit checks**: Run `pre_commit_instructions` and follow testing/verification.
6.  **Submit**: Commit and submit the changes with the message "change in frontend".
